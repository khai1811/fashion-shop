<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>
        <%= product.name %> | Fashion Store
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body>
    <%- include('../partials/header') %>
        <div class="container py-4">
            <a href="/products" class="back-link mb-3 d-inline-block">
                <i class="fa-solid fa-chevron-left"></i> Quay lại danh sách
            </a>
            <div class="row justify-content-center">
                <div class="col-lg-11">
                    <div class="product-detail-box">
                        <div class="row align-items-start">
                            <div class="col-md-5 mb-3 mb-md-0">
                                <img src="/images/<%= product.image %>" alt="<%= product.name %>"
                                    class="product-img w-100">
                            </div>
                            <div class="col-md-7">
                                <h2 class="fw-bold mb-3">
                                    <%= product.name %>
                                </h2>
                                <div class="mb-3 d-flex align-items-end">
                                    <span class="price-main">
                                        <%= product.price.toLocaleString('vi-VN') %>₫
                                    </span>
                                    <% if (product.oldPrice) { %>
                                        <span class="price-old">
                                            <%= product.oldPrice.toLocaleString('vi-VN') %>₫
                                        </span>
                                        <% } %>
                                </div>
                                <div class="mb-3 desc">
                                    <b>Mô tả:</b>
                                    <%= product.description || 'Chưa có mô tả.' %>
                                </div>

                                <form action="/cart/add/<%= product._id %>" method="get" class="mb-3"
                                    autocomplete="off">
                                    <div class="input-group mb-3" style="width: 170px;">
                                        <input type="number" class="form-control" name="qty" value="1" min="1"
                                            aria-label="Số lượng">
                                        <button class="btn btn-dark buy-btn" type="submit">
                                            <i class="fa-solid fa-cart-plus"></i> Thêm vào giỏ hàng
                                        </button>
                                    </div>
                                </form>
                                <div class="mb-3">
                                    <b>Danh mục:</b>
                                    <% if (product.category) { %>
                                        <span class="badge bg-secondary">
                                            <%= product.category %>
                                        </span>
                                        <% } %>
                                </div>
                                <div class="mb-2"><b>Chính sách:</b> Miễn phí đổi trả 15 ngày | Giao hàng toàn quốc |
                                    Chất lượng đảm bảo</div>
                                <div class="mt-3">
                                    <a href="/cart" class="btn btn-outline-primary me-2">
                                        <i class="fa fa-shopping-cart"></i> Xem giỏ hàng
                                    </a>
                                    <a href="/" class="btn btn-outline-secondary">Về trang chủ</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <%- include('../partials/footer') %>
</body>

</html>