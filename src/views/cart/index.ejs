<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>Thanh toán</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body>
    <%- include('../partials/header') %>
        <div class="container-fluid py-4">
            <div class="row">
                <!-- Bên trái: Thông tin đơn hàng -->
                <div class="col-lg-7">
                    <h5 class="mb-3 fw-bold">Thông tin đơn hàng</h5>
                    <!-- FORM CHUẨN, KHÔNG dropdown tỉnh/thành, method POST, action="/checkout" -->
                    <form action="/checkout" method="POST">
                        <div class="mb-2">
                            <input type="text" class="form-control" name="name" placeholder="Họ và tên" required>
                        </div>
                        <div class="mb-2">
                            <input type="text" class="form-control" name="phone" placeholder="Số điện thoại" required>
                        </div>
                        <div class="mb-2">
                            <input type="text" class="form-control" name="address" placeholder="Địa chỉ đầy đủ"
                                required>
                        </div>
                        <div class="mb-2">
                            <textarea class="form-control" name="note"
                                placeholder="Ghi chú giao hàng (tuỳ chọn)"></textarea>
                        </div>
                        <h6 class="my-3 fw-bold">Hình thức thanh toán</h6>
                        <div class="form-check mb-2">
                            <input class="form-check-input" type="radio" name="paymentMethod" id="cod" value="COD"
                                checked>
                            <label class="form-check-label" for="cod">
                                <span class="badge rounded-pill bg-dark me-2">COD</span> Thanh toán khi giao hàng (COD)
                            </label>
                        </div>
                        <div class="form-check mb-2">
                            <input class="form-check-input" type="radio" name="paymentMethod" id="vnpay" value="VNPAY">
                            <label class="form-check-label" for="vnpay">
                                <span class="badge rounded-pill bg-primary me-2">VNPAY</span> Ví điện tử VNPAY
                            </label>
                        </div>
                        <div class="form-check mb-2">
                            <input class="form-check-input" type="radio" name="paymentMethod" id="bank" value="BANK">
                            <label class="form-check-label" for="bank">
                                <span class="badge rounded-pill bg-danger me-2">Bank</span> Thanh toán Ngân Hàng
                            </label>
                        </div>
                        <button type="submit" class="btn btn-primary w-100 py-2 fw-bold mt-3">Đặt hàng</button>
                    </form>
                </div>
                <!-- Bên phải: Giỏ hàng, voucher, tổng kết -->
                <div class="col-lg-5">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h5 class="fw-bold mb-0"><i class="fa fa-shopping-cart text-primary me-2"></i> Giỏ hàng</h5>
                        <a href="/products" class="text-decoration-none">Sản phẩm</a>
                    </div>
                    <div class="card mb-3">
                        <div class="card-body">
                            <!-- ====================== DANH SÁCH SẢN PHẨM GIỎ HÀNG ====================== -->
                            <div class="cart-list mb-3">
                                <% if (cart && cart.items.length> 0) { %>
                                    <% cart.items.forEach(item=> { %>
                                        <div class="d-flex align-items-center border-bottom py-2 cart-item">
                                            <img src="/images/<%= item.image %>" alt="<%= item.name %>"
                                                style="width: 56px; height: 56px; object-fit: cover;"
                                                class="rounded shadow-sm me-3">
                                            <div class="flex-grow-1">
                                                <div class="fw-bold mb-1 text-truncate" style="max-width:180px;">
                                                    <%= item.name %>
                                                </div>
                                                <div class="small text-secondary">SL: <%= item.quantity %>
                                                </div>
                                            </div>
                                            <div class="fw-bold me-2" style="min-width:90px; text-align:right;">
                                                <%= item.price.toLocaleString('vi-VN') %>₫
                                            </div>
                                            <div class="d-flex align-items-center gap-1 ms-2">
                                                <!-- Xem sản phẩm -->
                                                <a href="/products/<%= item._id %>" class="btn btn-sm btn-light"
                                                    title="Xem sản phẩm">
                                                    <i class="fa-regular fa-eye"></i>
                                                </a>
                                                <!-- Xóa sản phẩm -->
                                                <form method="GET" action="/cart/remove/<%= item._id %>"
                                                    style="display:inline;">
                                                    <button type="submit" class="btn btn-sm btn-outline-danger"
                                                        title="Xóa sản phẩm">
                                                        <i class="fa-solid fa-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                        <% }) %>
                                            <% } else { %>
                                                <div class="alert alert-info py-2 mb-0">Giỏ hàng của bạn đang trống.
                                                </div>
                                                <% } %>
                            </div>
                            <!-- ==================== END DANH SÁCH SẢN PHẨM GIỎ HÀNG =================== -->
                            <hr>
                            <div class="mb-2 d-flex justify-content-between">
                                <span>Tạm tính:</span>
                                <span class="fw-bold">
                                    <span class="fw-bold">
                                        <%= (cart && typeof cart.totalPrice==='number' ) ?
                                            cart.totalPrice.toLocaleString('vi-VN') : '0' %>₫

                                    </span>
                                </span>
                            </div>
                            <div class="mb-2 d-flex justify-content-between">
                                <span>Phí vận chuyển:</span>
                                <span>Miễn phí</span>
                            </div>
                            <hr>
                            <span class="fw-bold">
                                <%= (cart && typeof cart.totalPrice==='number' ) ?
                                    cart.totalPrice.toLocaleString('vi-VN') : '0' %>₫
                            </span>

                        </div>
                    </div>
                </div>
            </div>
        </div>
        </div>
        <script src="https://cdn.jsdelivr.net/gh/duyplus/vue-address/dist/db.min.js"></script>
        <script src="/js/main.js"></script>
        <%- include('../partials/footer') %>
</body>

</html>